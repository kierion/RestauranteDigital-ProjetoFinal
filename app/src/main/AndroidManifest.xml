<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="trabalhofinal.usj.com.br.restaurantedigital">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:screenOrientation="portrait"
        android:configChanges="orientation"
        android:name=".activity.RestauranteDigitalActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity  android:name=".activity.DashboardActivity" />
        <activity android:name=".activity.ConfiguracoesActivity" />
        <activity android:name=".activity.MenuListActivity" />
                <activity  android:clearTaskOnLaunch="true" android:name=".activity.CadastroMenuListActivity"/>
        <activity android:name=".activity.CarrinhoActivity" />
        <activity android:name=".activity.GerenciarFuncionariosActivity" />
                <activity android:screenOrientation="portrait" android:configChanges="orientation" android:name=".activity.CadastroItemActivity" />
        <activity android:name=".activity.MenuDetalhadoActivity" />
    </application>
</manifest>